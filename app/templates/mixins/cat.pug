mixin cat(data)
	.cat(class = attributes.class)
		a.cat__img(href='#' style='background-image: url(img/' + data.img + ')')
		h4.cat__title= data.title
		p.cat__desc= data.desc
		a.btn-border(href='#') Shop now