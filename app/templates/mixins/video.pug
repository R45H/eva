mixin video(src, options)
	- if (!options) options = 'autoplay=1&showinfo=0&controls=0&modestbranding=1&rel=0';

	.video
		iframe.video__preview(src='https://www.youtube.com/embed/' + src + '?' + options frameborder='0' allowfullscreen)